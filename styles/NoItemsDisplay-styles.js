import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  cart: {
    alignSelf: 'center',
    height: 200,
    marginTop: 100,
    width: 250,
  },
  text: {
    marginTop: 60,
    textAlign: 'center',
  },
})

export default styles
