import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  addIcon: {
    alignSelf: 'flex-end',
    height: 80,
    marginBottom: 20,
    marginRight: 20,
    width: 80,
  },
})

export default styles
